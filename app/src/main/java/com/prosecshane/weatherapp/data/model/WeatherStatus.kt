package com.prosecshane.weatherapp.data.model

// Enum class, determines the weather status for an entry
enum class WeatherStatus {
    Clear,
    Cloudy,
    Overcast,
    Raining,
    Thunder,
    Snowing,
    Mist
}
